/**
 * Dados do tenant que a interface exibe. Hoje o tenant zero está literal no front, como
 * a marca em `PortalApp` e `App` — quando o segundo tenant entrar, isto vem do servidor
 * junto com o accent e o nome.
 */

/** WhatsApp da equipe, como o cliente fala com a Drakkar pelo portal. Com DDI: o
 * wa.me trata número sem o 55 como de outro país. */
export const TENANT_WHATSAPP = '+55 (48) 99999-8877';
